import { connect } from 'react-redux';
import Splash from './splash'

const mSTP = state => {
    return {
    
    }
}

const mDTP = dispatch => {
    return {
        
    }
}


export default connect(null, null)(Splash);